<tr>
  <td><%= product_counter+1 %></td>
  <td>
    <%= link_to product.name,
      dashboard_shop_product_path(product.shop_id, product.product_id) %>
  </td>
  <td><%= product.quantity %></td>
  <td><%= product.price.to_i %> <%= t "cart.vnd" %></td>
</tr>
