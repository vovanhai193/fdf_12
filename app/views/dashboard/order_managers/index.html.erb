<div class="container">
  <h2><%= t ".title" %></h2>
  <ul class="nav nav-tabs">
    <li class="active">
      <a data-toggle="tab" href="#done"><%= t ".done_orders" %></a>
    </li>
    <li>
      <a data-toggle="tab" href="#reject"><%= t ".reject_orders" %></a>
    </li>
  </ul>
  <div class="tab-content">
    <div id="done" class="tab-pane fade in active">
      <div class="well">
        <div class="panel-group">
          <% if @products_done.present? %>
            <%= render "product", products: @products_done %>
          <% else %>
            <span class="text-danger"><%= t ".not_products" %></span>
          <% end %>
        </div>
      </div>
    </div>
    <div id="reject" class="tab-pane fade">
      <div class="well">
        <div class="panel-group">
          <% if @products_reject.present? %>
            <%= render "product", products: @products_reject %>
          <% else %>
            <span class="text-danger"><%= t ".not_products" %></span>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
